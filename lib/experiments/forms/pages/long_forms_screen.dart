import 'package:flutter/material.dart';

class LongFormsScreen extends StatelessWidget {
  const LongFormsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Long Forms')),
      body: const Center(child: Text('Long Forms')),
    );
  }
}
