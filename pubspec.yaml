name: flutter_lab
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: 3.24.4

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  monarch: ^3.0.0
  build_runner: ^2.1.11

flutter:
  uses-material-design: true

  assets:
    - assets/
